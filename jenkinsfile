pipeline {
    agent any
    
    tools {
        // Definir la herramienta Node.js con el nombre 'nodejs'
        nodejs 'nodejs'
    }

    stages {
        stage('Checkout') {
            steps {
                script {
                    // Clonar el repositorio desde GitHub
                    git url: 'https://github.com/edansanmar/JavaScript-Capstone.git'
                    echo 'pasado'
                }
            }
        }

        stage('Build') {
            steps {
                script { 
                    // Puedes agregar aquí los pasos de construcción, como 'npm install'
                    sh 'npm install'
                }
            }
        }
    }
}
